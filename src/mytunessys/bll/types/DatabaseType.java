package mytunessys.bll.types;

/**
 * Predefined enum types for our database connections
 * because we are only using MSSQL the other enums are
 * there just as a demonstration what could be done with this current implementation
 */
public enum DatabaseType {
    MSSQL,
    TEXT_FILE
}
